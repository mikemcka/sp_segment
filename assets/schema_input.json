{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/WEHI-SODA-Hub/sp_segment/master/assets/schema_input.json",
    "title": "WEHI-SODA-Hub/sp_segment pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "run_backsub": {
                "type": "boolean",
                "default": false,
                "errorMessage": "Run backsub must be a boolean"
            },
            "run_mesmer": {
                "type": "boolean",
                "default": false,
                "errorMessage": "Run mesmer must be a boolean"
            },
            "run_cellpose": {
                "type": "boolean",
                "default": false,
                "errorMessage": "Run cellpose must be a boolean"
            },
            "run_cellsam": {
                "type": "boolean",
                "default": false,
                "errorMessage": "Run cellsam must be a boolean"
            },
            "tiff": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.tiff?$",
                "errorMessage": "TIFF file must exist, cannot contain spaces and must have extension '.tiff/.tif'"
            },
            "nuclear_channel": {
                "type": "string",
                "pattern": "^[\\w-\" ]+$",
                "errorMessage": "Nuclear channel must be provided as a single word"
            },
            "membrane_channels": {
                "type": "string",
                "pattern": "^[\\w-\" ]+(:[\\w-\" ]+)*$",
                "errorMessage": "Membrane channels must be provided as a colon-separated list"
            }
        },
        "required": ["sample", "tiff"]
    }
}
